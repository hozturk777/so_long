#include "so_long.h"

char	**read_map(char *map_path)
{
	int	fd;
	char	*line;
	char	**map;
	char	*temp;
	char	*joined_map;

	fd = open(map_path, O_RDONLY);
	if (fd < 0)
		return (NULL);
	joined_map = NULL;
	while (condition)
	{
		/* code */
	}
	
}